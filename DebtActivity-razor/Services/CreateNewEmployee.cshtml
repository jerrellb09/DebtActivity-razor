@{
    var firstName = Request.Form["firstNameAdd"];
    var lastName = Request.Form["lastname"];
    var position = Request.Form["position"];
    var telephone = Request.Form["telephone"];

    var db = Database.Open("centaur");

    var sql = @"INSERT into activedirectoryuser (firstname, lastname, position, telephone) values (@0, @1, @2, @3)";

    var results = db.Query(sql, firstName, lastName, position, telephone);

    var message = "User has been created";

    Json.Write(message, Response.Output);


}